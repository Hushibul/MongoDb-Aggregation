# Some basic stuff on MongoDB Aggregation

## Install all the package by using

npm install && npm install --save-dev

## To start the application

npm start

## If using docker

### Build

docker build -t mongodb-aggregation .

docker run mongodb-aggregation
